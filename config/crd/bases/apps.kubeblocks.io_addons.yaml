apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  labels:
    app.kubernetes.io/name: kubeblocks
  name: addons.apps.kubeblocks.io
spec:
  group: apps.kubeblocks.io
  names:
    kind: Addon
    listKind: AddonList
    plural: addons
    shortNames:
    - ad
    singular: addon
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: namespace for installed
      jsonPath: .spec.namespace
      name: NAMESPACE
      type: string
    - description: addon type
      jsonPath: .spec.type
      name: TYPE
      type: string
    - description: addon phase
      jsonPath: .status.phase
      name: PHASE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Addon is the Schema for the addons API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AddonSpec defines the desired state of Addon
            properties:
              defaultInstallValues:
                additionalProperties:
                  type: string
                description: DefaultInstallValues is the addon default install parameters.
                type: object
              dependencies:
                description: Dependencies lists the names of other Addons that must
                  be installed before this one.
                items:
                  type: string
                type: array
              description:
                description: Description is the description of the addon.
                type: string
              helm:
                description: Helm defines the helm chart spec for the addon
                properties:
                  chartLocationURL:
                    description: ChartLocationURL is the location of the helm chart.
                    type: string
                  chartName:
                    description: ChartName is the name of the helm chart.
                    type: string
                  chartVersion:
                    description: ChartVersion is the version of the helm chart.
                    type: string
                  values:
                    description: Values is the helm chart values in raw yaml.
                    type: string
                  valuesFiles:
                    description: ValuesFiles is the list of helm values files.
                    items:
                      type: string
                    type: array
                type: object
                x-kubernetes-preserve-unknown-fields: true
              install:
                description: Install defines the install spec for the addon
                properties:
                  enabled:
                    description: Enabled defines whether the addon is enabled
                    type: boolean
                  environment:
                    additionalProperties:
                      type: string
                    description: Environment is the environment variables to be set
                      for install.
                    type: object
                  helmInstallValues:
                    additionalProperties:
                      type: string
                    description: HelmInstallValues defines the helm install values
                      for the addon
                    type: object
                  resources:
                    description: Resources is a list of resources to be installed.
                    items:
                      type: string
                    type: array
                  serviceAccount:
                    description: ServiceAccount is the service account name to be
                      used for install.
                    type: string
                type: object
              namespace:
                description: Namespace is the namespace to install the helm chart.
                  if it's empty, will be installed to the same namespace as kubeblocks.
                type: string
              selector:
                description: Selector defines the selectors for the addon
                properties:
                  annotations:
                    additionalProperties:
                      type: string
                    description: Annotations is the selector to match addon annotations.
                    type: object
                  labelSelector:
                    description: LabelSelector is the label selector for the addon
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                  labels:
                    additionalProperties:
                      type: string
                    description: Labels is the selector to match addon labels.
                    type: object
                  namespaceSelector:
                    description: NamespaceSelector is the namespace selector for the
                      addon
                    properties:
                      matchExpressions:
                        description: matchExpressions is a list of label selector
                          requirements. The requirements are ANDed.
                        items:
                          description: |-
                            A label selector requirement is a selector that contains values, a key, and an operator that
                            relates the key and values.
                          properties:
                            key:
                              description: key is the label key that the selector
                                applies to.
                              type: string
                            operator:
                              description: |-
                                operator represents a key's relationship to a set of values.
                                Valid operators are In, NotIn, Exists and DoesNotExist.
                              type: string
                            values:
                              description: |-
                                values is an array of string values. If the operator is In or NotIn,
                                the values array must be non-empty. If the operator is Exists or DoesNotExist,
                                the values array must be empty. This array is replaced during a strategic
                                merge patch.
                              items:
                                type: string
                              type: array
                              x-kubernetes-list-type: atomic
                          required:
                          - key
                          - operator
                          type: object
                        type: array
                        x-kubernetes-list-type: atomic
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: |-
                          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                          map is equivalent to an element of matchExpressions, whose key field is "key", the
                          operator is "In", and the values array contains only "value". The requirements are ANDed.
                        type: object
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              type:
                description: Type is the type of the addon.
                type: string
            type: object
          status:
            description: AddonStatus defines the observed state of Addon
            properties:
              conditions:
                description: Conditions is an array of current observed addon conditions.
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              helmRelease:
                description: HelmRelease is the helm release name of the addon.
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this addon.
                format: int64
                type: integer
              phase:
                description: Phase is the phase of the addon
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
